
#include <bits/stdc++.h>
using namespace std;

int main()
{
    float num1,num2,sum,avg;
    
    cout<<"Enter two numbers\n";    
    
    cin>>num1>>num2;
    sum=num1+num2;
    avg=sum/2;
    
    cout<<"average of"<<num1<<" , "<<num2<<" is ="<<avg<<endl;

    return 0;
    
        /*  
        descriptions
        
        variable can be any single type of data container to hold the value
        
        if integer we say type integer
        
        if very big integer we say long or long long
        
        if decimal we say float
        
        if big ecimal we say double
        
        if binary true/false we say bool etc..
        
        
        cin>>var1>>var2 ...    reading multiple variable 
        
        similarly using cout<<string<<num1<<expressions ....
        
        multiple input or output we can do in a single shot
        
        
        this is called operator cascaing in c++
    
    */
    
}
